import Input.Parser
import Relude as R
import Data.Vector as V
import Data.Lang as D
parse
let l = it
let (Just x) = R.find ((== "Basque") . language) l
let g = buildGraph x
let (Graph v) = g

import IGraph as G
import IGraph.Algorithms.Centrality as C
let g1 = fromLabeledEdges [(e, 1) | e <- D.edges x] :: G.Graph 'G.U Int Int
let vertices = fromInteger $ D.nVertices x
let c = (1 / fromIntegral vertices) * sum $ C.closeness [0..vertices-1] g1 True (Just $ const 1)

closenessCentrality g

let c = bfs v 212
